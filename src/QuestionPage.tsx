import React from "react";
import Feature from "./Feature";

const QuestionPage: React.FC<{imageId: string, feature: Feature}> = ({imageId, feature}) => {
    return <div/>
}

export default QuestionPage